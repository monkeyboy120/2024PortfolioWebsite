---
import SmoothScroll from '../components/SmoothScroll.svelte';
import SectionReveal from '../components/SectionReveal.svelte';
---

<section id="hero" class="relative overflow-hidden px-4 pb-16 pt-28 sm:px-6 sm:pb-20 sm:pt-32">
  <div class="page-shell grid items-center gap-8 lg:grid-cols-[1.2fr_0.8fr] lg:gap-12">
    <SectionReveal client:load>
      <div class="max-w-3xl">
        <p class="eyebrow mb-5">Computer Engineering and Systems</p>
        <h1 class="font-display text-4xl leading-none text-ink sm:text-6xl lg:text-7xl">
          Software, systems, and robotics.
        </h1>
        <p class="section-copy mt-5 max-w-xl text-base sm:text-lg">
          I am Joshua La, a computer engineering student at Michigan interested in developer tools,
          low-level systems, and practical software.
        </p>
        <div class="mt-7 flex flex-col gap-3 sm:flex-row">
          <SmoothScroll targetId="#projects" client:load>
            <span class="inline-flex items-center justify-center rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white transition hover:bg-accentDeep">
              View Work
            </span>
          </SmoothScroll>
          <SmoothScroll targetId="#contact" client:load>
            <span class="inline-flex items-center justify-center rounded-full border border-line bg-surface px-6 py-3 text-sm font-semibold text-ink transition hover:border-accent hover:text-accent">
              Contact
            </span>
          </SmoothScroll>
        </div>
        <div class="mt-8 flex flex-wrap gap-2.5 text-sm text-muted">
          <span class="rounded-full border border-line bg-surface px-4 py-2">Robotics</span>
          <span class="rounded-full border border-line bg-surface px-4 py-2">Developer Tools</span>
          <span class="rounded-full border border-line bg-surface px-4 py-2">C++, Python, Rust</span>
        </div>
      </div>
    </SectionReveal>

    <SectionReveal client:load>
      <div class="relative mx-auto w-full max-w-xl animate-drift">
        <div class="absolute inset-0 -rotate-6 rounded-4xl bg-accentSoft/70 blur-3xl"></div>
        <div class="relative rounded-4xl border border-line bg-section-glow p-5 shadow-lift sm:p-8">
          <div class="grid gap-4 sm:grid-cols-2 sm:gap-6">
            <div class="rounded-3xl border border-line bg-surface p-5">
              <p class="text-sm uppercase tracking-[0.18em] text-accentDeep">Focus</p>
              <p class="mt-3 font-display text-xl text-ink sm:text-2xl">Clean, useful tools.</p>
              <p class="mt-3 text-sm leading-6 text-muted">
                Thoughtful UX backed by solid implementation.
              </p>
            </div>
            <div class="rounded-3xl border border-line bg-midnight p-5 text-white">
              <p class="text-sm uppercase tracking-[0.18em] text-accentSoft">Currently</p>
              <p class="mt-3 font-display text-xl sm:text-2xl">Learning through building.</p>
            </div>
            <div class="rounded-3xl border border-line bg-surface p-5 sm:col-span-2">
              <p class="text-sm uppercase tracking-[0.18em] text-accentDeep">Approach</p>
              <p class="mt-3 text-sm leading-6 text-muted sm:text-base sm:leading-7">
                I like building from first principles and refining until the result feels simple.
              </p>
            </div>
          </div>
        </div>
      </div>
    </SectionReveal>
  </div>
</section>
